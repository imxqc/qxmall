server:
  port: 9010

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.149.100:3306/qxmall_oms
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: qxmall-order
  rabbitmq:
    host: 192.168.149.100 # 你的虚拟机IP
    port: 15672 # 端口
    username: guest # 用户名
    password: guest # 密码
  thymeleaf:
    cache: false
  session:
    store-type: redis

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto